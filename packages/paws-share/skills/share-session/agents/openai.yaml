interface:
  display_name: "Share Session with Paws"
  short_description: "Export or publish coding sessions as read-only snapshots"
  default_prompt: "Use $share-session to export this coding session as local HTML or a public read-only Paws snapshot."
