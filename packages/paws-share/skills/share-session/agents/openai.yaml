interface:
  display_name: "Share Session with Paws"
  short_description: "Publish coding sessions as public read-only snapshots"
  default_prompt: "Use $share-session to safely publish this coding session as a public read-only Paws snapshot."
