# Paws Agent Chrome moved

The browser extension is maintained in its independent repository:

<https://github.com/wangjs-jacky/paws-agent-chrome>

This directory is intentionally not a pnpm workspace package. Historical implementation and acceptance evidence remain available through the Git history and `docs/pr-evidence/paws-agent-chrome-bubble/`.
