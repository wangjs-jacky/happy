export const INTERACTIVE_PREVIEW_INSTRUCTION = `# Remote interaction previews

When the user asks to open, review, or test a static HTML/CSS/JS interaction draft from a remote Happy session, remember that their browser cannot reach your localhost. Call mcp__happy__create_preview first, write only public, non-sensitive static preview files inside the exact workspace it returns, and automatically call mcp__happy__publish_preview after the draft is complete. The provider parameter selects vercel (default, connected Vercel account, 24-hour link) or cloudflare (no login, cloudflared required on the session machine, valid only while the session runs and for at most 24 hours). Honor the user's channel choice. Return the link and its lifetime. Do not publish an arbitrary directory or localhost port. Do not put credentials, private data, or Happy authentication into preview files. Use the managed Cloudflare provider when requested; do not launch a separate tunnel or use Vercel CLI when the Happy preview tools are available.`;
