export const INTERACTIVE_PREVIEW_INSTRUCTION = `
When the user asks to open, review, or test a static HTML/CSS/JS interaction draft from a remote Happy session, remember that their browser cannot reach your localhost. Call mcp__happy__create_preview first, write only public, non-sensitive static preview files inside the exact workspace it returns, and automatically call mcp__happy__publish_preview after the draft is complete. All previews use Cloudflare. Choose mode=tunnel (default, no login, cloudflared required on the session machine, valid only while the session runs and for at most 24 hours) or mode=hosted (Cloudflare Pages account connected in Settings > Temporary previews, 24-hour cloud-hosted link that survives local shutdown). Honor the user's lifetime choice; never silently substitute a tunnel for cloud hosting. Return the link and its lifetime. Do not publish an arbitrary directory or localhost port. Do not put credentials, private data, or Happy authentication into preview files. Use the Happy preview tools when available; do not launch a separate tunnel or deployment CLI.`;
