// Settings now render in the real app stack, including desktop modal descendants.
// Keep these aliases so existing settings screens share the native routing API.
export { useRouter as useSettingsRouter, useLocalSearchParams as useSettingsSearchParams } from 'expo-router';
