# Vercel interactive previews — PC Web evidence

| Case | Before | After | Acceptance |
|---|---|---|---|
| Vercel account binding | No managed Vercel preview setting existed. | `case-1-vercel-settings-after.png` | Shows account-scoped connected identity, OSS/Vercel lifecycle copy, and the 24-hour deadline. |
| Ego progress | Browser steps replaced the whole capability panel (user-supplied issue screenshot). | Covered by `BrowserStepsPopover.test.tsx` pending the authenticated session visual fixture. | Capability summary remains mounted; progress opens only from “查看过程” and closes explicitly. |
| Preview chat card | No typed preview event/card existed. | Covered by `typesRaw.spec.ts` plus `InteractivePreviewCard`. | Ready links only open externally or copy; no iframe and no interaction callback. |

The first screenshot is generated by `e2e/vercel-preview-evidence.spec.ts` at the fixed 1440×900 PC viewport. The Vercel API response is deterministic and contains no provider token.
