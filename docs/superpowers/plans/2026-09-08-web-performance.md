# Web performance repair

## Global Constraints

User approved fixing and verifying the diagnosed memory, sidebar, typing, loading and first-message startup issues together. Work only in this sibling worktree. Preserve existing user edits elsewhere and production services. Browser automation only through Ego. No test messages to existing real sessions. Keep stable wire identities, reading anchors, image cache semantics, outbox deduplication, pending permissions and account isolation. No silent data loss on submit, retry, refresh, or history eviction. Preserve native behavior unless a shared correctness bug requires a compatible fix. UI colors use theme tokens. User requested Before/After screenshots for long sidebar and first-message submission. Base 48b4aa5f73212110873bb40f4317fcd84410702f.

## Task 1: Draft and sidebar rendering

Fix useDraft cleanup saving previous text on every value change. True 2 second debounce for normal editing; empty/nonempty transition may save immediately. Latest text saved on blur/unmount/background; switching session must not write to another session; clearing sent draft must not resurrect old text. updateSessionDraft must not traverse and rebuild the entire sidebar on each text edit: update only target persisted draft and target session; update hasDraft row only when boolean changes, preserving unread flags and unaffected row references. Preserve drafts for unloaded sessions.

Projects and timeline currently wrap all active sessions in one FlatList item. Flatten group headers and visible session rows at the actual outer list level, preserving collapsible machines/projects, pinned order, selected row, location, contextual actions, bulk selection, scroll restoration, and existing styles. Do not hide sessions to fake performance. Lists mode already virtualizes; preserve it. Add behavior tests for debounce lifecycle and list structure with 1000 sessions, plus existing focused tests/typecheck. Commit product and test changes only. Report red/green commands and relevant outcomes.

## Task 2: Bounded Web history and transcript

Fix unbounded Web history retention in sync.ts (explicit pagination, retainMissingHistoryRows, live append), and disabled transcript virtualization. Introduce a shared bounded window policy: max 500 wire messages and approximate encrypted payload budget 8 MiB, always keep at least the anchor/one oversized message. Preserve navigation continuity and flags, allow older/newer reload from IndexedDB, preserve unseen gap boundaries. Retain at most 3 sessions as already implemented. Reproject safely on eviction without losing pending local messages or mutating live agent state from historical replay. Avoid replaying whole history on every incoming message; batch/trim only when capacity exceeds slack if safe, with a hard bounded ceiling. Use stable wire keys and existing reading restoration to keep viewport anchored when offscreen records are evicted.

Restore real Web virtualized rendering with bounded overscan, preserve image identity and avoid revoking in-use visible image URLs. Existing image pagination regressions must remain meaningful: update expectations of unlimited retention to bounded behavior, not delete assertions on visual continuity. Cover thousands of live messages, repeated older/newer pagination, oversized payload, historical island, receipt recovery, and images with focused regression tests/typecheck. If byte bound cannot safely cover reducer derived expansions, document it precisely. Commit changes and report evidence.

## Task 3: New-session first submission and startup

Implement responsive first-message submission in ComposeHome/useSpawnSession. Current sending covers machineSpawnNewSession, hydration and awaitLocalMessageProjection before clearing the draft. On submit, capture immutable text, attachment and mode snapshot into a recoverable pending submission; once safely saved release the composer and show a dedicated starting state with the submitted text, useful progress and failure retry/restore. Navigate to real session as soon as session registration and minimum required encryption snapshot allow, while message delivery/startup continues under an owner independent of the unmounted composer. Avoid duplicate creation/message on retry. If safe early routing requires too broad an architectural change, maintain a dedicated starting surface in ComposeHome until safe real route and keep composer released; that fulfills immediate feedback without inventing fake sessions.

Persist recoverable text/mode/session identity scoped to account/server; attachment recovery must either persist blobs in existing storage or explicitly retain original attachment selection until accepted and explain refresh limits without silently discarding content. Pending submission must survive refresh and not automatically send to a wrong account/session. Preserve edits typed after submit, ensure double-click submits once, ensure failures remain recoverable, and clear only accepted submission. No plaintext secrets to logs. Fix redundant startup work found on this path, but no unrelated CLI/server changes. Add timing stage metrics without prompt content and use focused tests with delayed spawn/hydration/upload/projection, failure/retry and unmount, account changes, refresh. Commit and report.

## Validation and delivery

After each implementation run task-scoped independent review. Then integrated typecheck and relevant app tests, real Ego browser Cases: C1 long sidebar scroll/click; C2 rapid typing/IME and draft lifecycle; C3 history pagination/long message retention/images; C4 first submit delayed start/retry; C5 cold/warm startup. Capture requested Before/After, final interaction video, and measurements. Use an isolated test harness/account, never mutate real conversations. Final whole-branch review and scoped interaction review. Create PR only after required checks; merge message confirmation follows repository rules. Report precise coverage and residual limits, never claim a 60 minute soak from short tests.
